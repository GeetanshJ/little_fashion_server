<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Order Summary</title>
  <style>
   * {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: "Inter", sans-serif;
  background-color: #f0f2f5;
  padding: 30px 15px;
  color: #333;
  line-height: 1.6;
}

.navbar {
  background-color: #fff;
  border-bottom: 1px solid #ddd;
  padding: 15px 30px;
  display: flex;
  justify-content: center;
}

.navbar-brand {
  font-size: 26px;
  font-weight: bold;
  color: #007bff;
  text-decoration: none;
}

.navbar-brand span {
  color: #000;
}

.cart-wrapper {
  max-width: 1000px;
  margin: 40px auto;
  background-color: #fff;
  padding: 30px;
  border-radius: 12px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.06);
}

.cart-container {
  display: flex;
  flex-direction: column;
  gap: 25px;
}

.cart-item {
  display: flex;
  gap: 20px;
  border-bottom: 1px solid #eee;
  padding-bottom: 20px;
}

.cart-item:last-child {
  border-bottom: none;
}

.cart-item img {
  width: 130px;
  height: 130px;
  object-fit: cover;
  border-radius: 10px;
  border: 1px solid #ddd;
}

.cart-details {
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 6px;
}

.cart-details div {
  font-size: 15px;
}

.summary-card {
  margin-top: 35px;
  padding: 25px;
  background-color: #f7f9fb;
  border-radius: 10px;
  font-size: 18px;
  font-weight: 500;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.summary-card span {
  font-weight: bold;
  color: #007bff;
}

.address-wrapper {
  background-color: #fff;
  padding: 30px;
  border-radius: 12px;
  border: 1px solid #ddd;
  max-width: 1000px;
  margin: 40px auto;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.04);
}

.address-wrapper h3 {
  margin-bottom: 20px;
  font-size: 22px;
  color: #222;
}

.address-details div {
  font-size: 15px;
  margin-bottom: 10px;
}

.container {
  max-width: 600px;
  margin: 40px auto;
  padding: 30px;
  background-color: #fff;
  border-radius: 10px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.06);
}

.container h2 {
  font-size: 24px;
  margin-bottom: 20px;
  color: #333;
}

label {
  font-size: 14px;
  font-weight: 600;
  color: #444;
  margin-bottom: 8px;
  display: block;
}

input[type="text"] {
  width: 100%;
  padding: 12px;
  font-size: 16px;
  margin-bottom: 20px;
  border: 1px solid #ccc;
  border-radius: 5px;
  transition: border-color 0.3s ease;
}

input[type="text"]:focus {
  border-color: #007bff;
  outline: none;
}

button {
  width: 100%;
  padding: 12px;
  background-color: #007bff;
  border: none;
  color: white;
  font-size: 16px;
  font-weight: bold;
  border-radius: 5px;
  cursor: pointer;
  transition: background 0.3s ease;
}

button:hover {
  background-color: #0056b3;
}

.card-info {
  text-align: center;
  font-size: 16px;
  font-weight: 500;
  margin-top: 20px;
  color: #555;
}

@media (max-width: 768px) {
  .cart-item {
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .cart-item img {
    width: 100%;
    height: auto;
  }

  .cart-details {
    align-items: center;
  }

  .summary-card {
    flex-direction: column;
    gap: 10px;
    text-align: center;
  }

  .address-wrapper, .cart-wrapper {
    padding: 20px;
  }
}

  </style>

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet" />
</head>

<body>
  <% let total = 0; %>

  <nav class="navbar">
    <a class="navbar-brand" href="/">
      <strong><span>Little</span> Fashion</strong>
    </a>
  </nav>

  <div class="cart-wrapper">
    <div class="cart-container">
      <% for (let i = 0; i < cartItems.length; i++) { total += cartItems[i].qty * cartItems[i].price; %>
      <div class="cart-item">
        <img src="/product_images/<%= cartItems[i].image %>" alt="Product Image" />
        <div class="cart-details">
          <div><strong>Name:</strong> <%= cartItems[i].name %></div>
          <div><strong>Description:</strong> <%= cartItems[i].description %></div>
          <div><strong>Price:</strong> $<%= cartItems[i].price %></div>
          <div><strong>Qty:</strong> <%= cartItems[i].qty %></div>
        </div>
      </div>
      <% } %>
    </div>

    <div class="summary-card">
      <div>Order Summary</div>
      <div>Total Price: <span>$<%= total %></span></div>
    </div>
  </div>

  <div class="address-wrapper">
    <h3>Shipping Address</h3>
    <div class="address-details">
      <div><strong>First Name:</strong> <%= addressInfo.firstname %></div>
      <div><strong>Last Name:</strong> <%= addressInfo.lastname %></div>
      <div><strong>Address:</strong> <%= addressInfo.address %></div>
      <div><strong>City:</strong> <%= addressInfo.city %></div>
      <div><strong>State:</strong> <%= addressInfo.state %></div>
      <div><strong>Country:</strong> <%= addressInfo.country %></div>
      <div><strong>Zip:</strong> <%= addressInfo.zip %></div>
      <div><strong>Phone:</strong> <%= addressInfo.phone %></div>
    </div>
  </div>

  <div class="card-info">
    CadNumber :XXXX<%=cardNumber%>
  </div>
  

  <a href="/checkout/pay/<%=addressId%>/<%=profileId%>/<%=paymentId%>" style="text-align: center;display: flex;justify-content: center;align-items: center;">Pay</a>
</body>

</html>
